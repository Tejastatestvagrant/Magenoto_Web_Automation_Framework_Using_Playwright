const { test, expect } = require("@playwright/test");

test.describe("login Feature Automation ", () => {
    test("login with null value ", ({ page }) => {
      
        page.goto('/');
        


  });
});
